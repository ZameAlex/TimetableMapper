@using TimeTableLibrary.FpmModels;
@model List<FpmGroup>
@{
    ViewData["Title"] = "Index";
}

<input list="groups"/>
<datalist id="groups">
	@foreach (var item in Model)
	{
		<option	value=@item.Name/>
	}
</datalist>

